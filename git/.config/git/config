# later, copy from: 
# https://github.com/GitAlias/gitalias/blob/master/gitalias.txt

[user]
	name = yashlala
	email = lala@lala
	signingkey = lala@lala
[interactive]
	singlekey = true
[merge]
	tool = vimdiff
[mergetool]
	keepBackup = false
	prompt = false
[diff]
	tool = vimdiff
[alias]
	ada = add --all
	ad = add
	adi = add --interactive
	adp = add --patch
	bl = blame -s
	br = branch
	bre = branch --edit-description
	brm = branch --move
	chb = checkout -b
	ch = checkout
	chp = checkout --patch
	coa = commit --amend
	co = commit
	coi = commit --interactive
	cp = cherry-pick
	di = diff --ignore-space-at-eol
	dis = diff --ignore-space-at-eol --staged
	dit = difftool
	diw = diff --ignore-space-at-eol --word-diff
	diws = diff --ignore-space-at-eol --word-diff --staged
	fe = fetch
	fep = fetch --prune
	gr = grep
	la = "!git config -l | grep alias | cut -c 7-" # list aliases
	loa = log --abbrev-commit --pretty=oneline --all
	lola = log --abbrev-commit --all
	lol = log --abbrev-commit
	lo = log --abbrev-commit --pretty=oneline
	me = merge
	met = mergetool
	pump = pull --merge --prune
	pum = pull --merge # pull-merge; *usually* default pull behavior
	pup = pull --prune
	pu = pull
	purp = pull --rebase --prune
	pur = pull --rebase
	rei = rebase --interactive
	re = rebase
	sh = show
	st = status
	tad = tag --delete
	ta = tag
	usp = reset --patch HEAD
	us = reset HEAD
[apply]
	whitespace = nowarn
[core]
	whitespace = -trailing-space
	editor = vim
[gui]
	fontui = -family \"DejaVu Sans\" -size 18 -weight normal -slant roman -underline 0 -overstrike 0
[advice]
	statusHints = false
	detachedHead = false
[init]
	templatedir = ~/.config/git/templates
[branch]
	autosetuprebase = always
