#!/bin/sh -e

exec cantata

# We now activate MPD via systemd socket. The lines below are no longer needed,
# and are preserved for their sentimental value. They comprised one of the
# first shell scripts I wrote.

# if ! mpc status >/dev/null 2>&1; then
#   mkdir -p ~/.local/share/mpd/
#   mpd &
# fi
#
# wait && exec ario
